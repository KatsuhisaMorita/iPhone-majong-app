name: Build iOS App

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    name: Build unsigned IPA
    runs-on: macos-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Build for Device (unsigned)
        run: |
          xcodebuild build \
            -project MahjongScoreApp.xcodeproj \
            -scheme MahjongScoreApp \
            -destination 'generic/platform=iOS' \
            -configuration Release \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_IDENTITY="" \
            PROVISIONING_PROFILE="" \
            CONFIGURATION_BUILD_DIR=$PWD/build/Release-iphoneos

      - name: Package as IPA
        run: |
          mkdir -p build/Payload
          cp -r build/Release-iphoneos/MahjongScoreApp.app build/Payload/
          cd build
          zip -r MahjongScoreApp.ipa Payload/

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: MahjongScoreApp-ipa.zip
          path: build/MahjongScoreApp.ipa
